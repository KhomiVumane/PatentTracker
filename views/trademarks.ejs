<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>InnovaShield – Trademarks</title>
  <link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet"/>
  <style>
    :root {
      --bg: #0a0e1a; --surface: #111827; --card: #1a2236; --border: #2a3a55;
      --accent: #3b82f6; --accent2: #06b6d4; --text: #e2e8f0; --muted: #64748b;
      --success: #10b981; --danger: #ef4444; --warning: #f59e0b;
    }
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { background: var(--bg); color: var(--text); font-family: 'DM Sans', sans-serif; min-height: 100vh; }
    nav { background: var(--surface); border-bottom: 1px solid var(--border); padding: 0 2rem; display: flex; align-items: center; gap: 2rem; height: 60px; position: sticky; top: 0; z-index: 100; }
    .logo { font-family: 'Syne', sans-serif; font-weight: 800; font-size: 1.2rem; color: var(--accent); }
    nav a { color: var(--muted); text-decoration: none; font-size: 0.9rem; font-weight: 500; padding: 0.3rem 0.8rem; border-radius: 6px; transition: all 0.2s; }
    nav a:hover, nav a.active { color: var(--text); background: var(--card); }
    .container { max-width: 1200px; margin: 0 auto; padding: 2rem; }
    .page-header { margin-bottom: 2rem; }
    .page-header h1 { font-family: 'Syne', sans-serif; font-size: 2rem; font-weight: 800; }
    .page-header p { color: var(--muted); margin-top: 0.3rem; }
    .card { background: var(--card); border: 1px solid var(--border); border-radius: 12px; padding: 1.5rem; margin-bottom: 2rem; }
    .card h2 { font-family: 'Syne', sans-serif; font-size: 1.1rem; font-weight: 700; margin-bottom: 1.2rem; color: var(--accent2); }
    .form-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 1rem; }
    .form-group { display: flex; flex-direction: column; gap: 0.4rem; }
    .form-group label { font-size: 0.8rem; font-weight: 500; color: var(--muted); text-transform: uppercase; letter-spacing: 0.5px; }
    .form-group input, .form-group select { background: var(--surface); border: 1px solid var(--border); border-radius: 8px; padding: 0.6rem 0.9rem; color: var(--text); font-family: 'DM Sans', sans-serif; font-size: 0.9rem; transition: border-color 0.2s; }
    .form-group input:focus, .form-group select:focus { outline: none; border-color: var(--accent); }
    .form-group select option { background: var(--surface); }
    .btn { padding: 0.6rem 1.4rem; border-radius: 8px; border: none; font-family: 'DM Sans', sans-serif; font-weight: 500; font-size: 0.9rem; cursor: pointer; transition: all 0.2s; }
    .btn-primary { background: var(--accent); color: #fff; }
    .btn-primary:hover { background: #2563eb; }
    .btn-warning { background: rgba(245,158,11,0.15); color: var(--warning); border: 1px solid var(--warning); }
    .btn-warning:hover { background: rgba(245,158,11,0.25); }
    .btn-sm { padding: 0.35rem 0.9rem; font-size: 0.8rem; }
    table { width: 100%; border-collapse: collapse; }
    thead th { text-align: left; font-size: 0.75rem; text-transform: uppercase; letter-spacing: 0.5px; color: var(--muted); padding: 0.6rem 1rem; border-bottom: 1px solid var(--border); }
    tbody tr { border-bottom: 1px solid var(--border); transition: background 0.15s; }
    tbody tr:hover { background: rgba(59,130,246,0.05); }
    tbody td { padding: 0.85rem 1rem; font-size: 0.9rem; }
    .badge { display: inline-block; padding: 0.2rem 0.7rem; border-radius: 20px; font-size: 0.75rem; font-weight: 600; }
    .badge-active { background: rgba(16,185,129,0.15); color: var(--success); }
    .badge-expired { background: rgba(239,68,68,0.15); color: var(--danger); }
    .badge-pending { background: rgba(245,158,11,0.15); color: var(--warning); }
    .badge-renewed { background: rgba(59,130,246,0.15); color: var(--accent); }
    .form-actions { margin-top: 1.2rem; }
    .edit-banner { background: rgba(245,158,11,0.08); border: 1px solid rgba(245,158,11,0.3); border-radius: 8px; padding: 0.7rem 1rem; margin-bottom: 1rem; font-size: 0.85rem; color: var(--warning); }
  </style>
</head>
<body>
<nav>
  <span class="logo">⚡ InnovaShield</span>
  <a href="/patents">Patents</a>
  <a href="/trademarks" class="active">Trademarks</a>
  <a href="/users">Users</a>
  <a href="/watchlist">Watchlist</a>
  <a href="/ownership">Ownership</a>
</nav>

<div class="container">
  <div class="page-header">
    <h1>Trademarks</h1>
    <p>View and update trademark records in the system.</p>
  </div>

  <!-- EDIT FORM -->
  <% if (typeof editTrademark !== 'undefined' && editTrademark) { %>
  <div class="card">
    <h2>✏️ Edit Trademark</h2>
    <div class="edit-banner">Editing: <strong><%= editTrademark.name %></strong></div>
    <form action="/trademarks/update" method="POST">
      <input type="hidden" name="trademark_id" value="<%= editTrademark.trademark_id %>"/>
      <div class="form-grid">
        <div class="form-group">
          <label>Name</label>
          <input type="text" name="name" value="<%= editTrademark.name %>" required/>
        </div>
        <div class="form-group">
          <label>Owner</label>
          <input type="text" name="owner" value="<%= editTrademark.owner %>" required/>
        </div>
        <div class="form-group">
          <label>Industry</label>
          <input type="text" name="industry" value="<%= editTrademark.industry %>"/>
        </div>
        <div class="form-group">
          <label>Status</label>
          <select name="status">
            <option value="Active" <%= editTrademark.status==='Active'?'selected':'' %>>Active</option>
            <option value="Expired" <%= editTrademark.status==='Expired'?'selected':'' %>>Expired</option>
            <option value="Pending" <%= editTrademark.status==='Pending'?'selected':'' %>>Pending</option>
            <option value="Renewed" <%= editTrademark.status==='Renewed'?'selected':'' %>>Renewed</option>
          </select>
        </div>
      </div>
      <div class="form-actions">
        <button type="submit" class="btn btn-primary">Save Changes</button>
      </div>
    </form>
  </div>
  <% } %>

  <!-- LIST -->
  <div class="card">
    <h2>All Trademarks</h2>
    <table>
      <thead>
        <tr><th>#</th><th>Name</th><th>Owner</th><th>Industry</th><th>Registered</th><th>Expiry</th><th>Status</th><th>Action</th></tr>
      </thead>
      <tbody>
        <% trademarks.forEach(t => { %>
        <tr>
          <td><%= t.trademark_id %></td>
          <td><%= t.name %></td>
          <td><%= t.owner %></td>
          <td><%= t.industry %></td>
          <td><%= t.registration_date ? new Date(t.registration_date).toLocaleDateString() : '-' %></td>
          <td><%= t.expiry_date ? new Date(t.expiry_date).toLocaleDateString() : '-' %></td>
          <td><span class="badge badge-<%= t.status ? t.status.toLowerCase() : '' %>"><%= t.status %></span></td>
          <td>
            <a href="/trademarks/edit/<%= t.trademark_id %>" class="btn btn-warning btn-sm">Edit</a>
          </td>
        </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</div>
</body>
</html>
